export default function App() {
  // Placeholder — replaced by the router + screens after the fixtures checkpoint.
  return (
    <div style={{ padding: 40 }}>
      <span className="font-display" style={{ fontSize: 15, color: '#07111F' }}>
        Union
      </span>
    </div>
  );
}
